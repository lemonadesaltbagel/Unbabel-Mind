# Server Configuration
PORT=3001

# Database Configuration (use 'localhost' for local dev, 'postgres' for Docker)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=unbabel
DB_USER=postgres
DB_PASSWORD=password

# Security
JWT_SECRET=your-secret-key

# OpenAI API
OPENAI_API_KEY=your_openai_api_key_here 
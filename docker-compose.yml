version: '3.8'
services:
  postgres:
    image: postgres:17
    container_name: unbabel-db
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: unbabel
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - unbabel-network
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.dev
    container_name: unbabel-backend
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=development
      - PORT=3001
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=unbabel
      - DB_USER=postgres
      - DB_PASSWORD=password
      - JWT_SECRET=your-secret-key
    depends_on:
      - postgres
    networks:
      - unbabel-network
    volumes:
      - ./backend:/app
      - /app/node_modules
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    container_name: unbabel-frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - NEXT_PUBLIC_API_URL=http://localhost:3001/api
    depends_on:
      - backend
    networks:
      - unbabel-network
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
networks:
  unbabel-network:
    driver: bridge
volumes:
  postgres_data: 